

      <h4>
        サブスクリプションメニュー(全<%= @subscriptions.count %>件)
      </h4>
 
    <% @subscriptions.each do |subscription| %>
     <p><%= subscription.title %></p>
     <p><%= subscription.descript %></p>
     <p><% tax_in = subscription.price * 1.10 %></p>
     <p>¥ <%= tax_in.round.to_s(:delimited) %>（税込）</p>
     <%= link_to "詳細",users_subscription_path(subscription.id) %>
    <% end %>
