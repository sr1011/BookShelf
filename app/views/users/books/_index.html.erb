<table>
    <thead>
        <tr>
          <th></th>
          <th>タイトル</th>
          <th>説明</th>
          <th></th>
          <th></th>
          <th></th>
        </tr>
    </thead>
    <tbody id="book">
    <% books.each do |book| %>
      <tr>
        <th><%= attachment_image_tag book, :image, :fill, 100, 100, format: 'jpeg' %></th>
        <td><%= book.name %></td>
        <td><%= book.author %></td>
        <td><%= link_to "詳細",users_book_path(book.id), class:"btn btn-primary" %></td>
          <td></td>
          <td></td>
      </tr>
    <% end %>
    </tbody>
</table>